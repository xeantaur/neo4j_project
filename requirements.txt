# Network Traffic & Security Alert Analysis — Dependencies
# Requires Python 3.10+ (Python 3.9 is EOL)
#
# Ingestion pipeline uses pandas and standard Python modules (no JVM/Java requirement).
# Backend API uses FastAPI and Uvicorn.
# Version strategy: bounded dependency versions for compatibility.

pandas>=2.0,<3.0
neo4j>=5.14,<6.0
python-dotenv>=1.0,<2.0
pytest>=7.0,<10.0
fastapi>=0.110,<1.0
uvicorn>=0.28,<1.0
httpx>=0.27,<1.0
